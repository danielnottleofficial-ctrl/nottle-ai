{
  "name": "nottle-ai",
  "version": "1.0.0",
  "private": true,
  "description": "NOTTLE AI Android application",
  "scripts": {
    "android:web": "node scripts/prepare-android-web.mjs",
    "android:sync": "npm run android:web && cap sync android",
    "android:bundle": "npm run android:sync && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
